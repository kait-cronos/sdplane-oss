
log stdout
# debug sdplane tap-handler
# debug shell
# debug sdplane telnet-opt
debug sdplane enhanced-repeater
debug sdplane rib

set worker lthread stat-collector
set worker lthread rib-manager

set mempool

stop port all
set port all dev-configure 1 4
set port all nrxdesc 1024
set port all ntxdesc 1024

set worker lcore 1 enhanced-repeater
set worker lcore 2 l3-tap-handler

set port all promiscuous enable
start port all

set thread 1 port 0 queue 0
set thread 1 port 1 queue 0

sleep 1
update port status
start worker lcore all

# queue config should be after rib-manager.

set vswitch 1234 vlan 1234
set vswitch 1234 port 0 tagged
set vswitch 1234 port 1 tagged

sleep 1
